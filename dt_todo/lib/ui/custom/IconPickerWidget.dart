import 'package:dt_todo/helper/IconHelper.dart';
import 'package:flutter/material.dart';

import 'package:dt_todo/helper/IconHelper.dart';
/*
class IconPickerWidget extends StatefulWidget {
  @override
  _IconPickerWidgetState createState() => _IconPickerWidgetState();
}

class _IconPickerWidgetState extends State<IconPickerWidget> {
  IconData _curIcon = null;
  @override
  Widget build(BuildContext context) {
    return PopupMenuButton<IconData>(
        icon: (_curIcon == null) ? Icon(Icons.tag_faces) : Icon(_curIcon),
        onSelected: choiceAction,
        itemBuilder: (BuildContext context) {
          return IconHelper.choices.map((IconData _icon) {
              return PopupMenuItem(
                value: _icon,
                child: Icon(_icon),
              );
          }).toList();
        },
    );
  }

  void choiceAction(IconData _icon) {
    setState(() {
      _curIcon = _icon;
    });
  }
}


*/

